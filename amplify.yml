version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands: 
        - yarn webpack
        - echo "API=$API" >> ./dist/.env
  artifacts:
    baseDirectory: ./dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
